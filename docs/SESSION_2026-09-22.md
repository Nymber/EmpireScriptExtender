# Session 2026-09-22

Source of truth is now `EmpireScriptExtender/src/ese_proxy.c`. The toolkit `ESE/` folder was a second copy and has been removed. `empire.ps1` builds and deploys from `src/`. Do not recreate `ESE/`.

## First person

Camera fields are live: eye `+0x08/0x0C/0x10`, look `+0x14/0x18/0x1C`, forward `+0x60/0x64/0x68`. Manager is `s:137D488 + 0x31C`. `+0x28048` does not change when a unit is selected. The unit under the camera is the entity cluster, excluding the marker sitting on the camera.

`FPFRESH` drops the cached camera when that manager pointer changes. A rebuild forces first person off until `=` is pressed again. Do not write the camera on the frame the pointer changes.

Deployment rejects march orders with a beep. Do not send them before the battle starts. Do not use the deployment proxy `s:5F6E00`.

## Battle state is not found yet

Watching `lua_setfield` for `CameraZoomTo` never fired. Binding on any of 208 battle names bound a UI state (`SelectionChanged`). That state was freed as the battle loaded. The next `lua_gettop` crashed at static `00F07504`.

A later build only bound if `CameraZoomTo` was already a function. Those six names are not passed to `lua_getfield` at all.

The call probe hooked the wrong address (`0x00B07850`, a file offset). The log line is `REFUSING to hook 00D27850`. The real `lua_pcall` is static `00F07850`, prologue `57 8B 7C 24 10`. The corrected hook is in `src/ese_proxy.c` and has not been deployed. It logs the first 24 calls and does not bind a state.

## Left as-is

Stock Controls hide/show packs were swapped earlier and not retested. `zz_chain.pack` was not modified. Rollback for the stock tab is deleting `data/zz_stocktab.pack`.
